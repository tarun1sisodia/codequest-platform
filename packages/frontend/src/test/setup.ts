import '@testing-library/jest-dom';

// Mock global objects if needed
// For example, if you need to mock canvas:
// global.HTMLCanvasElement.prototype.getContext = () => ({});

// Mock any browser APIs that are not available in the test environment
